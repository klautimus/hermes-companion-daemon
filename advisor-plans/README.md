# Companion Audit v5 — Implementation Plans

Generated by Atlas on 2026-06-16 from a fresh end-to-end audit of:
- Android app at `~/.hermes/projects/HermesCompanion` (HEAD 0a30d82)
- Daemon at `~/.hermes/companion` (HEAD d378902)

This audit was triggered by the user's "fresh end-to-end review" request after the v4 board's GATE self-reported clean with fabricated test counts (56 pass / 49 skipped — surface coverage was actually 35%).

## Critical discoveries the prior 5 boards missed

| # | Issue | Discovered by | Plan |
|---|-------|---------------|------|
| 1 | **Daemon not running in production** — no systemd unit installed, port 8777 unbound, cloudflared tunnel down. Android app cannot reach Hermes via companion. | DEPLOY-1 | 001 |
| 2 | **QR setup token broken end-to-end** — daemon generates `token=`, Android parses `pass=` only. Plan 003's security improvement non-functional. | DEPLOY+SEC | 002 |
| 3 | **49 security-critical tests permanently disabled** — scrypt auth, password gen, QR, first-run wizard have zero active coverage. | TEST-1, TEST-2 | 004 |
| 4 | **EncryptedSharedPreferences silent fallback to plaintext** — credentials stored in plaintext on affected devices. | CORR-3, SEC-005 | 003 |
| 5 | **Generated password printed to stdout** — undermines Plan 003's QR token security goal. | SEC-006 | 008 |

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bring the companion daemon up — install systemd unit, fix package metadata, deploy | P0 | M | — | TODO |
| 002 | Fix the QR setup-token end-to-end — Android must parse `token=` | P0 | S | 001 | TODO |
| 003 | Fail closed on EncryptedSharedPreferences — no silent plaintext fallback | P0 | M | — | TODO |
| 004 | Re-enable 49 security-critical skipped tests | P0 | M | — | TODO |
| 005 | Fix `pip install` package metadata | P1 | S | — | TODO |
| 006 | Synchronize `_SETUP_TOKENS` to prevent concurrent-redeem race | P1 | S | — | TODO |
| 007 | Sanitize subprocess error messages — don't leak daemon internals to clients | P1 | S | — | TODO |
| 008 | Stop printing generated password to stdout in setup wizard | P0 | S | — | TODO |
| 009 | Fix setup token time-mixing bug — use `time.monotonic()` consistently | P1 | S | — | TODO |
| 010 | Stream attachment downloads + close HermesProxy session | P1 (stream) / P2 (cleanup) | S | — | TODO |
| 011 | Add Android release signing + data extraction rules | P1 | M | — | TODO |
| 012 | Add rate limiting to /api/setup/redeem + per-username lockout | P2 | M | 006 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 must complete first** — the daemon being up is the floor for end-to-end verification of plans 002 (QR token), 006 (token sync), 007 (error messages), 012 (rate limit).
- **006 must complete before 012** — 012 adds per-IP rate limit inside the same lock that 006 introduces.
- Plans 003, 004, 005, 008, 009, 010, 011 are independent of each other.
- The Android plans (002, 003, 011) are independent of the daemon plans (001, 004, 005, 006, 007, 008, 009, 010, 012) except where noted.

## File-conflict graph (runtime topology override)

The "Depends on" column above is the LOGICAL dependency graph. The runtime topology is a separate concern — even when two plans have no logical dependency, they cannot run in parallel if they edit the same files.

| Plan | Files modified | Conflicts with |
|------|----------------|----------------|
| 001 | daemon: setup.py, pyproject.toml, systemd/*.service, ~/.config/systemd/user/* | — (no shared files) |
| 002 | android: MainActivity.kt, QrScannerScreen.kt, SetupWizardScreen.kt, MainViewModel.kt, ApiClient.kt, *Test.kt | — (only touches QR + redeem flow) |
| 003 | android: SessionManager.kt, SetupWizardScreen.kt, SettingsScreen.kt, *Test.kt | — (only touches storage mode flow) |
| 004 | daemon: tests/test_setup_wizard.py, tests/test_first_run.py | — (test files only) |
| 005 | daemon: setup.py, pyproject.toml | 001 (both touch setup.py + pyproject.toml) |
| 006 | daemon: server.py, tests/test_setup_token.py | 007, 009, 010, 012 (all touch server.py) |
| 007 | daemon: server.py, tests/test_server.py | 006, 009, 010, 012 (all touch server.py) |
| 008 | daemon: setup_wizard.py | — (single line) |
| 009 | daemon: server.py | 006, 007, 010, 012 (all touch server.py) |
| 010 | daemon: server.py | 006, 007, 009, 012 (all touch server.py) |
| 011 | android: app/build.gradle.kts, app/src/main/AndroidManifest.xml, res/xml/* (NEW) | — (only touches release config) |
| 012 | daemon: server.py, tests/test_auth_hardening.py | 006, 007, 009, 010 (all touch server.py) |

**Runtime topology (combining logical + file conflicts)**:

```
Phase 1 (parallel — independent files):
  003, 004, 008, 011

Phase 2 (serial — daemon package metadata, only one plan at a time):
  001 → 005

Phase 3 (serial — daemon server.py, all plans touch it):
  006 → 007 → 009 → 010 → 012

Phase 4 (Android, parallel):
  002 (depends on 001 for end-to-end test, but code is Android-only so can implement in parallel with Phase 1-3)
```

## Findings considered and rejected

| Finding | Source | Rejected because |
|---------|--------|------------------|
| **"Two divergent copies of every module"** | v4 CORR-5 | Re-verified: `src/hermes_companion/server.py` does not exist. The "divergence" was a v4 fabrication. Only root `server.py` exists. (Confirmed by `ls /home/kevin/.hermes/companion/src/hermes_companion/` showing only `__init__.py`, `systemd/`, `templates/`.) |
| **`config.py` vs `config_schema.py` duplication** | TEST-7 | `config.py` is legacy but appears unused at runtime. Out of scope for this audit; left for future cleanup. Will be picked up if the user does a "config cleanup" plan. |
| **Android release variant build issues** | DEPLOY-5 | Addressed as part of Plan 011 (release signing + data extraction rules). |
| **pip-audit on Android dependencies** | SEC deferred | Not run — would require `./gradlew dependencyCheckAnalyze` setup. Out of scope. |
| **CORS configuration on daemon** | SEC deferred | aiohttp defaults to no CORS (secure). If a frontend needs cross-origin access, address in a separate plan. |
| **Android certificate pinning** | SEC deferred | Not implemented. OkHttpClient uses system trust store. If MITM is a real threat, address in a future security hardening pass. |

## Rejected subagent claims (verified incorrect)

- **CORR-5 (v4) "two divergent implementations"** — DEPLOY audit verified this is false. `src/hermes_companion/server.py` does not exist. Only one copy of the daemon code (root `server.py`).
- **CORR-6 line numbers** — Off by ~50 lines. Real location is `server.py:638-665`, not the line numbers the subagent cited. The bug is still real.
- **CORR-1 line numbers for Android SessionMigration** — Subagent cited wrong file path. Real path is `org/hermes/community/companion/MainActivity.kt:34-36` (subagent had the `com.atlas.hermescompanion` path which was renamed).

## Verification baseline (run before any plan lands)

```bash
# Daemon
cd /home/kevin/.hermes/companion
python3 -m pytest tests/ 2>&1 | tail -3
# Expected: 56 passed, 49 skipped (per current state)

# Android
cd /home/kevin/.hermes/projects/HermesCompanion
./gradlew assembleDebug --no-daemon 2>&1 | tail -5
# Expected: BUILD SUCCESSFUL

# Deploy
ss -tlnp 2>/dev/null | grep -E "8642|8777"
# Expected: only 8642 listening (Hermes API up, companion DOWN)
```

## After all 12 plans land

The GATE worker (last card on the autonomous kanban board) MUST re-verify all 4 baselines and re-run every "Done criteria" command from every plan. The GATE must refuse completion if any exit code is non-zero.

Watchdog cron job must independently re-run all baseline commands every 30 minutes and diff against GATE's self-reported counts.
