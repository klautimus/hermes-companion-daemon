# Implementation Plans — Hermes Companion Daemon

Generated by the improve skill on 2026-06-16. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

**Planned at commits**: daemon=`f78cd82`, android=`47041ea`

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Consolidate daemon to single implementation (root → canonical) | P1 | M | — | DONE |
| 002  | Auth hardening: constant-time compare + brute-force + scrypt N bump | P1 | M | 001 | DONE |
| 003  | Fix QR code password leak (replace with one-time setup token) | P1 | S | 001 | DONE |
| 004  | Android credentials: migrate DataStore → EncryptedSharedPreferences | P1 | M | 001, 002 | DONE |
| 005  | Android cleartext traffic: add network_security_config.xml | P1 | S | 001 | DONE |
| 006  | Attachment upload: stream read with early size abort | P2 | S | 001 | DONE |
| 007  | Config schema: add type coercion/validation in from_dict | P2 | S | 001 | DONE |
| 008  | Android test rewrite: characterization tests for ViewModel | P2 | L | 001 | DONE |
| 009  | Security headers middleware + CORS docs | P3 | S | 001 | DONE |

## Dependency notes

- **001 must complete first** — all daemon-side plans (002, 003, 006, 007, 009) touch the same root `server.py`/`config_schema.py`/`setup_wizard.py` files. The consolidation determines the canonical codebase.
- **002 before 004** — auth hardening on daemon side (constant-time compare, rate limiting) must land before Android re-encrypts stored credentials; otherwise rotated credentials just get re-exposed.
- **004 + 005 together** — fixing cleartext without encrypting storage leaves credentials exposed on device; fixing storage without cleartext leaves them exposed in transit. Do both in same session.
- **008 before any ViewModel refactor** — current tests are "does not crash" no-ops (CORR-08). Characterization tests must land before any state-flow changes.

## Findings considered and rejected

| Finding | Reason |
|---------|--------|
| CORR-03 (async hazards) | Addressed by 008 (characterization tests) before any state refactor; premature fix without tests is unsafe. |
| CORR-04 (error swallowing) | Low-leverage logging fix; defer until after 008 establishes test baseline. |
| CORR-09 (attachment path traversal) | Root already has `os.path.basename` + `..`→`_` + `att_` random prefix. Defense-in-depth; not critical. |
| CORR-10 (env injection) | `shell=False` + list args + existing `_validate_slug` (root L343-347) is adequate. |
| CORR-11 (SSE timeout) | Non-streaming v1; streaming deferred to v2 per README. |
| SEC-08 (HERMES_KANBAN_BOARD env) | Same as CORR-10; slug validation exists. |
| SEC-09 (username timing) | scrypt cost ~100ms dwarfs dict lookup; accept risk. |
| SEC-10 (API 33+ media perms) | App only uses images; current perms correct. |
| SEC-12 (.env fallback for API key) | Daemon prefers env vars; .env is backward compat. Document, don't fix. |
| SEC-13 (ProGuard serialization) | kotlinx.serialization requires serializer companions kept; acceptable trade-off. |
| Perf/Deps findings | Interrupted audit; if critical, will surface in next recursive gate after 001-005. |